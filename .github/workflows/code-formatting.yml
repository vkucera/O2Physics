---
name: Formatting

'on': [pull_request_target]

permissions: {}

jobs:
  formatting:
    name: PR formatting
    uses: vkucera/ali-bot/.github/workflows/c++-code-formatting.yml@format-includes
    secrets:
      alibuild_github_token: ${{ secrets.ALIBUILD_GITHUB_TOKEN }}
